import Text from "./Text";
import Button from "./Button";
import IconButton from "./IconButton";

const components = { Text: Text, Button: Button, IconButton: IconButton };

export default components;
